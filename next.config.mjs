/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "fakestoreapi.com",
      "cxwittvlafaseejgksuy.supabase.co",
      "img.freepik.com",
    ],
  },
};

export default nextConfig;
